spring:
  application:
    name: elastic-product
  profiles:
    active: production
  elasticsearch:
    rest:
      uris: http://localhost:9200

eureka:
  instance:
    appname: elasticsearch-service
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/